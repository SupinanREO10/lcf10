import { sql } from "drizzle-orm";
import { integer, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const responses=sqliteTable("responses",{id:integer("id").primaryKey({autoIncrement:true}),role:text("role").notNull(),province:text("province").notNull(),participation:text("participation").notNull(),ratings:text("ratings").notNull(),expectation:text("expectation").notNull(),actualResult:text("actual_result").notNull(),keep:text("keep").notNull().default(""),gapReason:text("gap_reason").notNull().default(""),successFactor:text("success_factor").notNull().default(""),obstacle:text("obstacle").notNull().default(""),improvement:text("improvement").notNull(),application:text("application").notNull().default(""),support:text("support").notNull().default(""),additional:text("additional").notNull().default(""),timeline:text("timeline").notNull(),nextAction:text("next_action").notNull(),adoptionStatus:text("adoption_status").notNull().default(""),successIndicator:text("success_indicator").notNull().default(""),driveSynced:integer("drive_synced",{mode:"boolean"}).notNull().default(false),createdAt:text("created_at").notNull().default(sql`CURRENT_TIMESTAMP`)})
